{
  "scripts": {
    "build": "esbuild index.js --define:INCLUDE_TESTS=false --minify --outfile=dist/index.min.js",
    "test": "node index-creator.js -t"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
